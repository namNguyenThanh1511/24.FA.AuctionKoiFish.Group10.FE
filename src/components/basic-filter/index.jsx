import { SearchOutlined } from "@ant-design/icons";
import { Form, Input, Select, Tag } from "antd";
import React from "react";

export default function BasicFilter({ filterItems }) {
  return <div>{filterItems}</div>;
}
